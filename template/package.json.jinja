{
  "private": true,
  "description": "{{ project_description }} — npm dependencies only; see Makefile for build targets.",
  "dependencies": {
    "@johnnymorganz/stylua-bin": "^2.3.1",
    "prettier": "^3.8.1"
  }
}
